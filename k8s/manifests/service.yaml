apiVersion: v1
kind: Service
metadata:
  name: chat-app
  labels:
    app: chat-app
spec:
  type: ClusterIP
  selector:
    app: chat-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      # NodePort: 30080