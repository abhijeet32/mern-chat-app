apiVersion: v1
kind: Service
metadata:
  name: chat-app
  labels:
    app: chat-app
spec:
  type: NodePort
  selector:
    app: chat-app
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080